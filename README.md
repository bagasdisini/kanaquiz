# Kana Quiz
Kana Quiz made with React.js.

This is a legacy project which is no longer maintained.

New version is available at https://kana.pro/


Install deps: `npm install`

Development: `npm start`

Production: `npm run build`
